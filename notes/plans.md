# May 31, 2025

## Testing the SDRAM controller:
1. For this test (and also in general), I wish to use an `LcvStall`
"Address Space Slicer" module of some sort.
    1. This will (combinatorially?) divide an address space using some of
    the high bits of an address to select an `LcvStall` bus device to
    access.
    1. This is the opposite
1. 
